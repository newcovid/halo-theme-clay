# Settings reference

> Generated by `scripts/gen-settings-reference.py` from `i18n-settings/settings.en.yaml` — do not edit by hand.
> That file is what ships as `settings.yaml` inside the English package, so this
> reference is exported from the source rather than translated from the Chinese one.

<!-- markdownlint-disable -->

18 groups, 318 settings.

| Group | Label | Settings |
| --- | --- | --- |
| `global` | Global | 19 |
| `styles` | Style | 134 |
| `index_styles` | Home page style | 47 |
| `post_styles` | Post page style | 31 |
| `categories_page_styles` | Category page style | 6 |
| `category_page_styles` | Category details page style | 4 |
| `tags_page_styles` | Tag Collection Page Style | 6 |
| `tag_page_styles` | Tag details page style | 4 |
| `author_page_styles` | Author details page style | 3 |
| `archives_page_styles` | Archives page style | 5 |
| `custom_page_styles` | Custom page style | 8 |
| `error_page_styles` | Error Page Style | 4 |
| `sns` | Social media settings/RSS | 8 |
| `share` | Custom Share Buttons | 10 |
| `links_page_styles` | Links Page Style | 3 |
| `photos_styles` | Photo Page Style | 12 |
| `moments_styles` | Moments page style | 6 |
| `friends_page_styles` | Friends page style | 8 |

## Global

Access in templates: `theme.config.global.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `default_content_language` | text | Default content language | `zh-CN` | Default content language, e.g., zh-CN or en. If this field is left empty, zh-CN will be used by default. To switch languages, add ?language=language_code to the end of the URL, for example: https:/… |
| `is_i18n_prefix_match_mode` | switch | I18n prefix match mode | on | If enabled, the multi-language feature will use prefix match mode. See documentation for details. |
| `is_sync_language_cookie_to_content_language` | switch | Sync Cookie language based on page content language | on | When enabled, avoids fixed page text provided by the theme using a different language from the content, but may cause an extra browser refresh. |
| `is_auto_redirect_to_browser_language` | switch | Auto redirect to browser language | off | If enabled, when the browser language is different from the default content language and a corresponding language page exists, it will automatically redirect to the corresponding language page. |
| `auto_redirect_target_language_list` | object array | Allowed target region language code list for redirect | — | When multilingual prefix match mode is disabled, matching follows the browser language candidate path order. When enabled, earlier items are matched first. (shown only when `$is_auto_redirect_to_br… |
| └ `v` | text | Region language code | — | e.g. zh-CN, zh-TW, zh-Hans, zh-Hant, en, en-US |
| `is_i18n_menu_show` | switch | Enable multi-language menu support | off | If enabled, the theme will automatically generate a multi-language menu based on the menu name. For details, please refer to the documentation. |
| `upgrade_insecure_requests` | switch | CSP:upgrade-insecure-requests | off | If enabled, the theme will add the upgrade-insecure-requests directive to the Content-Security-Policy header. |
| `anti_mirror_site` | switch | Anti-mirror site | off | If enabled, the theme will check the domain name of the current page. If the domain name is not in the whitelist, it will jump to the target link. |
| `allow_site_whitelist` | object array | Domain whitelist | — | (shown only when `$anti_mirror_site === true`) |
| └ `input_domain` | text | Domain (base64 encoded) | — | For example, the domain name is example.com, the base64 encoding is ZXhhbXBsZS5jb20= |
| `target_url` | text | Target link (base64 encoded) | — | When the domain name of the current page is not in the whitelist, the theme will jump to the target link. (shown only when `$anti_mirror_site === true`) |
| `is_keep_path_and_query` | switch | Keep path and query | on | If enabled, the theme will keep the path and query parameters of the current page when jumping to the target link. (shown only when `$anti_mirror_site === true`) |
| `is_performance_monitor_enable` | switch | Enable performance monitor panel | off | If enabled, the theme will inject the performance monitor panel script into the page. |
| `is_instant_page_enable` | switch | instant.page support | off | If enabled, the theme will automatically load the instant.page script to improve the page loading speed. |
| `is_mermaid_enable` | switch | Mermaid support | off | If enabled, the theme will automatically load the Mermaid script |
| `mermaid_content_scope_selector` | text | Mermaid content scope selector | `main` | Only look for Mermaid diagrams inside areas matched by this CSS selector. (shown only when `$is_mermaid_enable === true`) |
| `mermaid_extra_source_element_selector` | text | Mermaid extra source element selector | — | Use CSS selectors to additionally mark elements that should be rendered as Mermaid diagrams. Matched elements read diagram source from textContent; elements with the auto class render both light an… |
| `mermaid_config` | code | Mermaid Config Properties | `{ startOnLoad: false }` | Docs: https://mermaid.js.org/config/schema-docs/config.html#mermaid-config-properties (shown only when `$is_mermaid_enable === true`) |

## Style

Access in templates: `theme.config.styles.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `is_cjk_serif_webfont_enable` | switch | Bundled CJK Serif Font | on | Display headings and blockquotes use a serif face, but whether a Chinese serif exists at all depends on the visitor's device — Windows, macOS and iOS ship one, Android does not, so Chinese headings… |
| `is_custom_font_files_enable` | switch | Enable Custom Font Files | off | Enable this option to use uploaded custom font files instead of the default fonts |
| `custom_font_configs` | object array | Custom font files | — | (shown only when `$is_custom_font_files_enable === true`) |
| └ `font_style` | select | Font style | `normal` | https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@font-face/font-style |
| └ `custom_font_style` | text | Custom Font Style Value | `oblique 30deg 50deg` | (shown only when `$get(font_style).value === 'custom'`) |
| └ `font_weight` | select | Font weight | `400` | https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@font-face/font-weight |
| └ `custom_font_weight` | text | Custom Font Weight Value | `1 1000` | (shown only when `$get(font_weight).value === 'custom'`) |
| └ `font_display` | select | Font Display Strategy | `swap` | https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@font-face/font-display |
| └ `font_unicode_range` | text | Font Unicode Range | — | Specify the Unicode range supported by the font, e.g., U+000-5FF (Latin and Basic Latin Supplement), U+4E00-9FFF (CJK Unified Ideographs), etc. Leave blank to support all Unicode characters. Docume… |
| └ `font_file` | attachment | Select font file | — | Upload .woff2/.woff/.ttf/.otf/.eot/.ttc/.otc/.sfnt font files. The theme will prioritize these files to replace the default fonts. |
| `custom_font_name` | text | Font Name (Full Name/PostScript Name) | — | Enter the font's internal "Full Name (nameID=4)" or "PostScript Name (nameID=6)", e.g., My Custom Font Regular or MyCustomFont-Regular. You may leave this blank if unknown. If left blank, even if t… |
| `is_click_effect_enable` | switch | Enable Click Effect | on | Draws a hairline mark at the pointer on mouse-down and fades it out. Skipped for touch input, and for visitors whose system asks for reduced motion. |
| `is_click_effect_secondary_enable` | switch | Also Show the Effect on Secondary Click | on | A circle for the primary button, a rounded square for the secondary one — the two halves of the site mark. Turn off to keep the primary button only. (shown only when `$is_click_effect_enable === tr… |
| `cursor_style` | select | Cursor Style | `clay` | The built-in set is 13 SVG pointers designed with the theme, one variant per color scheme, switched automatically. No extra requests. |
| `is_show_unused_cursor_files` | switch | Show Unused Cursor File Configurations | off | Generally, there is no need to enable this option. These pointers have no place to apply in this theme, so uploading them has no effect. (shown only when `$cursor_style === 'custom'`) |
| `custom_cursor_files_auto` | attachment | Select Auto Cursor File (CSS declared as auto) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default auto cursor. Description: Automatically selects the appropriate cursor based on context, can be set as the… |
| `custom_cursor_files_default` | attachment | Select Default Cursor File (CSS declared as default) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default cursor. Description: The default cursor, usually an arrow. (shown only when `$cursor_style === 'custom'`) |
| `custom_cursor_files_context_menu` | attachment | Select Context Menu Cursor File (CSS declared as context-menu) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default context menu cursor. Description: Indicates a context menu is available. (shown only when `$cursor_style =… |
| `custom_cursor_files_help` | attachment | Select Help Cursor File (CSS declared as help) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default help cursor. Description: Indicates help is available. (shown only when `$cursor_style === 'custom'`) |
| `custom_cursor_files_pointer` | attachment | Select Pointer Cursor File (CSS declared as pointer) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default pointer cursor. Description: Usually a hand when hovering over a link. (shown only when `$cursor_style ===… |
| `custom_cursor_files_progress` | attachment | Select Progress Cursor File (CSS declared as progress) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default progress cursor. Description: The program is busy in the background, but the user can still interact (oppo… |
| `custom_cursor_files_wait` | attachment | Select Wait Cursor File (CSS declared as wait) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default wait cursor. Description: The program is busy, and the user cannot interact (opposite of progress). The ic… |
| `custom_cursor_files_cell` | attachment | Select Cell Cursor File (CSS declared as cell) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default cell cursor. Description: Indicates a cell can be selected. (shown only when `$cursor_style === 'custom'`) |
| `custom_cursor_files_crosshair` | attachment | Select Crosshair Cursor File (CSS declared as crosshair) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default crosshair cursor. Description: Crosshair cursor, usually indicates selection in a bitmap. (shown only when… |
| `custom_cursor_files_text` | attachment | Select Text Cursor File (CSS declared as text) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default text cursor. Description: Indicates text can be selected. (shown only when `$cursor_style === 'custom'`) |
| `custom_cursor_files_vertical_text` | attachment | Select Vertical Text Cursor File (CSS declared as vertical-text) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default vertical-text cursor. Description: Indicates vertical text can be selected. (shown only when `$cursor_styl… |
| `custom_cursor_files_alias` | attachment | Select Alias Cursor File (CSS declared as alias) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default alias cursor. Description: Copy or shortcut will be created. (shown only when `$cursor_style === 'custom' … |
| `custom_cursor_files_copy` | attachment | Select Copy Cursor File (CSS declared as copy) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default copy cursor. Description: Indicates copy. (shown only when `$cursor_style === 'custom' && $is_show_unused_… |
| `custom_cursor_files_move` | attachment | Select Move Cursor File (CSS declared as move) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default move cursor. Description: The hovered object can be moved. (shown only when `$cursor_style === 'custom'`) |
| `custom_cursor_files_no_drop` | attachment | Select No Drop Cursor File (CSS declared as no-drop) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default no-drop cursor. Description: The current position cannot be dropped. (shown only when `$cursor_style === '… |
| `custom_cursor_files_not_allowed` | attachment | Select Not Allowed Cursor File (CSS declared as not-allowed) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default not-allowed cursor. Description: Action not allowed. (shown only when `$cursor_style === 'custom'`) |
| `custom_cursor_files_grab` | attachment | Select Grab Cursor File (CSS declared as grab) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default grab cursor. Description: Can be grabbed. (shown only when `$cursor_style === 'custom'`) |
| `custom_cursor_files_grabbing` | attachment | Select Grabbing Cursor File (CSS declared as grabbing) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default grabbing cursor. Description: Grabbing. (shown only when `$cursor_style === 'custom'`) |
| `custom_cursor_files_all_scroll` | attachment | Select All Scroll Cursor File (CSS declared as all-scroll) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default all-scroll cursor. Description: Element can be scrolled (panned) in any direction. (shown only when `$curs… |
| `custom_cursor_files_col_resize` | attachment | Select Column Resize Cursor File (CSS declared as col-resize) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default column resize cursor. Description: Element can be resized in width. Usually rendered as two arrows separat… |
| `custom_cursor_files_row_resize` | attachment | Select Row Resize Cursor File (CSS declared as row-resize) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default row resize cursor. Description: Element can be resized in height. Usually rendered as two arrows separated… |
| `custom_cursor_files_n_resize` | attachment | Select North Resize Cursor File (CSS declared as n-resize) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default north resize cursor. Description: One edge will be moved. For example, se-resize is used when the southeas… |
| `custom_cursor_files_e_resize` | attachment | Select East Resize Cursor File (CSS declared as e-resize) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default east resize cursor. Description: One edge will be moved. For example, se-resize is used when the southeast… |
| `custom_cursor_files_s_resize` | attachment | Select South Resize Cursor File (CSS declared as s-resize) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default south resize cursor. Description: One edge will be moved. For example, se-resize is used when the southeas… |
| `custom_cursor_files_w_resize` | attachment | Select West Resize Cursor File (CSS declared as w-resize) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default west resize cursor. Description: One edge will be moved. For example, se-resize is used when the southeast… |
| `custom_cursor_files_ne_resize` | attachment | Select Northeast Resize Cursor File (CSS declared as ne-resize) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default northeast resize cursor. Description: One edge will be moved. For example, se-resize is used when the sout… |
| `custom_cursor_files_nw_resize` | attachment | Select Northwest Resize Cursor File (CSS declared as nw-resize) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default northwest resize cursor. Description: One edge will be moved. For example, se-resize is used when the sout… |
| `custom_cursor_files_se_resize` | attachment | Select Southeast Resize Cursor File (CSS declared as se-resize) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default southeast resize cursor. Description: One edge will be moved. For example, se-resize is used when the sout… |
| `custom_cursor_files_sw_resize` | attachment | Select Southwest Resize Cursor File (CSS declared as sw-resize) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default southwest resize cursor. Description: One edge will be moved. For example, se-resize is used when the sout… |
| `custom_cursor_files_ew_resize` | attachment | Select East-West Resize Cursor File (CSS declared as ew-resize) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default east-west resize cursor. Description: Indicates bidirectional resizing. Applied to sliders (input[type=ran… |
| `custom_cursor_files_ns_resize` | attachment | Select North-South Resize Cursor File (CSS declared as ns-resize) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default north-south resize cursor. Description: Indicates bidirectional resizing. (shown only when `$cursor_style … |
| `custom_cursor_files_nesw_resize` | attachment | Select Northeast-Southwest Resize Cursor File (CSS declared as nesw-resize) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default northeast-southwest resize cursor. Description: Indicates bidirectional resizing. (shown only when `$curso… |
| `custom_cursor_files_nwse_resize` | attachment | Select Northwest-Southeast Resize Cursor File (CSS declared as nwse-resize) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default northwest-southeast resize cursor. Description: Indicates bidirectional resizing. (shown only when `$curso… |
| `custom_cursor_files_zoom_in` | attachment | Select Zoom-In Cursor File (CSS declared as zoom-in) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default zoom-in cursor. Description: Indicates zooming in. (shown only when `$cursor_style === 'custom' && $is_sho… |
| `custom_cursor_files_zoom_out` | attachment | Select Zoom-Out Cursor File (CSS declared as zoom-out) | — | Upload .cur/.png/.svg cursor files. The theme will prioritize these files over the default zoom-out cursor. Description: Indicates zooming out. (shown only when `$cursor_style === 'custom' && $is_s… |
| `color_schema` | select | Color scheme | `auto` | If the "Color Scheme Toggle Button" is enabled, this determines the color scheme when the website is first loaded. |
| `custom_color_schema_init_id` | number | Custom Color Scheme Identifier | — | Please create a custom color scheme first, then fill in the custom color scheme ID here. (shown only when `$color_schema === 'custom'`) |
| `custom_color_schema` | object array | Custom color scheme | — |  |
| └ `id` | number | Custom color scheme identifier | `1` | Unique identifier, please do not duplicate |
| └ `type` | select | Theme color scheme | `dark` |  |
| └ `is_css_variable_mode` | switch | CSS Variable Mode | off | If enabled, CSS variables will be used to define the color scheme. After toggling this option, please click the submit button below, then reopen this item to load the sub-options normally. |
| └ `primary_color` | color | Primary Color | `#2bbc8a` | Primary brand color, used for buttons, links, and other core interactive elements, corresponding to CSS variable --color-primary. (shown only when `$get(is_css_variable_mode).value === false`) |
| └ `primary_content_color` | color | Primary Content Color | `#212326` | Text/icon color under primary color, corresponding to CSS variable --color-primary-content. (shown only when `$get(is_css_variable_mode).value === false`) |
| └ `secondary_color` | color | Secondary Color | `#ccffb6` | Natural auxiliary color, used for secondary buttons or backgrounds, corresponding to CSS variable --color-secondary. (shown only when `$get(is_css_variable_mode).value === false`) |
| └ `secondary_content_color` | color | Secondary Content Color | `#d5d7d8` | Text/icon color on secondary color, corresponding to CSS variable --color-secondary-content. (shown only when `$get(is_css_variable_mode).value === false`) |
| └ `accent_color` | color | Accent Color | `#d480aa` | High-contrast accent color, used for embellishments and status reminders, corresponding to CSS variable --color-accent. (shown only when `$get(is_css_variable_mode).value === false`) |
| └ `accent_content_color` | color | Accent Content Color | `#212326` | Foreground content color under accent color, corresponding to CSS variable --color-accent-content. (shown only when `$get(is_css_variable_mode).value === false`) |
| └ `neutral_color` | color | Neutral Color | `#1d1f21` | Base color for unsaturated parts in UI, corresponding to CSS variable --color-neutral. (shown only when `$get(is_css_variable_mode).value === false`) |
| └ `neutral_content_color` | color | Neutral Content Color | `#d5d7d8` | Foreground color on neutral background, corresponding to CSS variable --color-neutral-content. (shown only when `$get(is_css_variable_mode).value === false`) |
| └ `base_100_color` | color | Base 100 Color | `#212326` | Main canvas background of html, corresponding to CSS variable --color-base-100. (shown only when `$get(is_css_variable_mode).value === false`) |
| └ `base_200_color` | color | Base 200 Color | `#1c1c1c` | Deeper background layer, corresponding to CSS variable --color-base-200. (shown only when `$get(is_css_variable_mode).value === false`) |
| └ `base_300_color` | color | Base 300 Color | `#181818` | Even deeper layer, corresponding to CSS variable --color-base-300. (shown only when `$get(is_css_variable_mode).value === false`) |
| └ `base_content_color` | color | Base Content Color | `#d5d7d8` | Foreground content on base color, corresponding to CSS variable --color-base-content. (shown only when `$get(is_css_variable_mode).value === false`) |
| └ `is_raw_css_output_mode` | switch | Raw CSS Output Mode | off | When this is disabled, you only need to fill in the custom CSS variables part, and the output will automatically be in the corresponding CSS selector (selector is html[theme="theme-{id}"] (replace … |
| └ `custom_css` | code | Custom CSS Variables | — | Examples in the documentation (shown only when `$get(is_css_variable_mode).value === true`) |
| `is_show_color_scheme_toggle_button` | switch | Color Scheme Toggle Button | off | (After toggling this option, please click the save button below and then refresh the page.) If enabled, a light/dark mode toggle button will appear next to the main title. It will automatically cyc… |
| `theme_auto` | select | Auto Mode Color Scheme | `auto` | (shown only when `$is_show_color_scheme_toggle_button===true`) |
| `custom_theme_auto_id` | number | Custom Color Scheme Identifier | — | Please create a custom color scheme first, then fill in the custom color scheme ID here. (shown only when `$is_show_color_scheme_toggle_button == true && $theme_auto === 'custom'`) |
| `theme_light` | select | Light Mode Color Scheme | `light` | (shown only when `$is_show_color_scheme_toggle_button===true`) |
| `custom_theme_light_id` | number | Custom Color Scheme Identifier | — | Please create a custom color scheme first, then fill in the custom color scheme ID here. (shown only when `$is_show_color_scheme_toggle_button == true && $theme_light === 'custom'`) |
| `theme_dark` | select | Dark Mode Color Scheme | `dark` | (shown only when `$is_show_color_scheme_toggle_button===true`) |
| `custom_theme_dark_id` | number | Custom Color Scheme Identifier | — | Please create a custom color scheme first, then fill in the custom color scheme ID here. (shown only when `$is_show_color_scheme_toggle_button == true && $theme_dark === 'custom'`) |
| `is_auto_switch_color_scheme_based_on_browser_settings` | switch | Automatically switch color scheme based on browser settings | on | If enabled, it will first read the saved color scheme selection from the browser. If there is no saved selection, it will automatically switch to light/dark mode based on the browser theme settings… |
| `is_save_color_scheme_settings_to_browser` | switch | Save color scheme settings to browser | on | If enabled, the current color scheme selection will be saved to the browser storage after pressing the color scheme toggle button. (shown only when `$is_show_color_scheme_toggle_button===true`) |
| `text_size` | select | Text size | `normal` |  |
| `inline_code_style` | select | Inline code style | `dotted-border` | Select the global inline code style. The default keeps the dotted border from the base `code` style, while other options replace it with alternative highlight styles without affecting multi-line co… |
| `dark_content_text_style` | select | Dark content text style | `soft-gray` | Select the text style used for dark-mode content text. `Soft Gray` makes body text feel softer in dark mode while making the contrast between darker text/headings and body text more pronounced, red… |
| `is_max_width_settings` | switch | Custom content area maximum width | on | If enabled, you can customize the maximum width of the content area. |
| `max_width` | text | Maximum width of content area | `48rem` | Allow all CSS length units, such as: 48rem, 780px, 70vw, 70%. (shown only when `$is_max_width_settings===true`) |
| `is_min_width_settings` | switch | Custom content area minimum width | off |  |
| `min_width` | text | Minimum width of content area | `48rem` | Allow all CSS length units, such as: 48rem, 780px, 70vw, 70%. When the screen width is less than the minimum width, content area will be less than the minimum width. (shown only when `$is_min_width… |
| `is_force_min_width_settings` | switch | Force content area minimum width | off | If enabled, the content area will always be larger than the minimum width. So when the screen width is less than the minimum width, vertical scrolling will appear. (shown only when `$is_min_width_s… |
| `is_content_width_style_settings` | switch | Custom content area width style | off | This determines the content area width style. |
| `content_width_style` | text | Width style of content area | `fit-content` | This determines the style value of the width attribute corresponding to the content area style. (shown only when `$is_content_width_style_settings===true`) |
| `is_show_header_icon` | switch | Header icon | off |  |
| `icon` | attachment | Custom header icon | — | (shown only when `$is_show_header_icon===true`) |
| `avatar_circle` | switch | Circle avatar | off | (shown only when `$is_show_header_icon===true`) |
| `avatar_grayout` | switch | Avatar grayscale | off | (shown only when `$is_show_header_icon===true`) |
| `extra_menu_items` | object array | Extra menu items | `[{'type': 'search'}]` | Add extra items to the menu |
| └ `type` | select | Menu item type | `search` |  |
| `is_show_header_menu` | switch | Header menu | on |  |
| `is_show_page_number` | switch | Display page number | on |  |
| `is_footer_site_stats_show` | switch | Site statistics information at the bottom of the page | off |  |
| `footer_site_stats` | object array | Statistics settings | `[{'type': 'visit', 'i18n_text': True, 'icon': ''}, {'type': 'post', 'i18n_text': True, 'icon': ''}, {'type': 'upvote', 'i18n_text': True, 'icon': ''}, {'type': 'comment', 'i18n_text': True, 'icon': ''}, {'type': 'category', 'i18n_text': True, 'icon': ''}, {'type': 'wordcount', 'i18n_text': True, 'icon': ''}]` | (shown only when `$is_footer_site_stats_show === true`) |
| └ `type` | select | Statistics item | — |  |
| └ `i18n_text` | switch | Wrap text in multiple languages | on |  |
| └ `icon` | icon | Icon to the left of the text | — |  |
| `is_footer_theme_info_show` | switch | Theme information in the footer | on |  |
| `footer_theme_info_theme_name` | radio | Theme name displayed in the footer theme information | `Clay` | (shown only when `$is_footer_theme_info_show === true`) |
| `footer_theme_info_halo_version_name` | radio | Halo version displayed in the footer theme information | `Halo` | (shown only when `$is_footer_theme_info_show === true`) |
| `is_footer_copyright_show` | switch | Footer theme copyright information in the footer | on |  |
| `footer_copyright_custom_name` | text | Custom copyright signature | — | If left blank, the site title will be used as the signature (shown only when `$is_footer_copyright_show === true`) |
| `is_footer_beian_show` | switch | Footer ICP filing information | off | For sites hosted in mainland China. The theme renders it in the current color scheme, so there is no need to hand-write a fixed-color HTML snippet into the footer content. |
| `footer_beian_icp` | text | ICP filing number | — | Copy it exactly as issued, e.g. 京ICP备12345678号-1. Leave blank to hide this line. (shown only when `$is_footer_beian_show === true`) |
| `footer_beian_icp_url` | text | ICP filing number link | `https://beian.miit.gov.cn/` | Defaults to the MIIT filing system at https://beian.miit.gov.cn/ (shown only when `$is_footer_beian_show === true`) |
| `footer_beian_police` | text | Public security filing number | — | Copy it exactly as issued, e.g. 京公网安备11010502030143号. Leave blank to hide this line. (shown only when `$is_footer_beian_show === true`) |
| `footer_beian_police_url` | text | Public security filing number link | `https://beian.mps.gov.cn/` | Defaults to the national internet security service platform at https://beian.mps.gov.cn/ (shown only when `$is_footer_beian_show === true`) |
| `footer_beian_police_icon` | attachment | Icon shown before the public security number | — | Optional. Upload the badge image issued with the filing; leave blank for text only. (shown only when `$is_footer_beian_show === true`) |
| `is_footer_force_bottom` | switch | Force Footer at Page Bottom | on |  |
| `is_footer_menu_show` | switch | Footer menu | on |  |
| `is_footer_content_show` | switch | Add content to page bottom/sidebar | off | When enabled, choose whether content is shown inline at the page bottom or as a floating sidebar corner in Content display mode. |
| `footer_content` | code | Page bottom/sidebar content | — | support HTML code block (shown only when `$is_footer_content_show === true`) |
| `is_18n_footer_content_show` | switch | Multi-language page bottom/sidebar content support | off | When enabled, you can configure page bottom/sidebar content for different languages. |
| `i18n_footer_content` | object array | Custom multi-language page bottom/sidebar content | — | (shown only when `$is_18n_footer_content_show === true`) |
| └ `lang` | text | Language code | `zh-CN` |  |
| └ `footer_content` | code | Page bottom/sidebar content | — | support HTML code block |
| `footer_content_sidebar_position` | select | Content display mode | `inline` | Choose 'Inline (page bottom)' to render at the page bottom, or select a corner to render as a floating sidebar. (shown only when `$is_footer_content_show === true \|\| $is_18n_footer_content_show =… |
| `footer_content_sidebar_opacity` | number | Sidebar hover opacity | `1` | Range: 0 (fully transparent) to 1 (fully opaque). (shown only when `($is_footer_content_show === true \|\| $is_18n_footer_content_show === true) && $footer_content_sidebar_position !== 'inline'`) |
| `footer_content_sidebar_narrow` | select | Tablet and mobile sidebar display behavior | `hide` | Display behavior when viewport width is less than 1024px. (shown only when `($is_footer_content_show === true \|\| $is_18n_footer_content_show === true) && $footer_content_sidebar_position !== 'inl… |
| `is_h3_underline` | switch | Add underline to H3 headings | on | If enabled, an underline will be displayed below H3 headings to make them stand out more. |
| `is_preserve_empty_lines_in_blockquote` | switch | Preserve empty lines in blockquote | on | If enabled, empty lines in blockquotes will be preserved; otherwise, they will be automatically removed. Blockquotes are represented by the > symbol in Markdown. |
| `is_show_the_quote_before_blockquote` | switch | Show the quote before Blockquote | on | If enabled, the theme will display the quote before the blockquote. In Markdown, you can use the > symbol to create a blockquote. |
| `is_show_the_quote_after_blockquote` | switch | Show the quote after Blockquote | off | If enabled, the theme will display the quote after the blockquote. In Markdown, you can use the > symbol to create a blockquote. |
| `is_show_the_table_bottom_border` | switch | Table bottom border for each row (excluding the header) | off | If enabled, a border will be added to the bottom of each table row. |
| `table_bottom_border_width` | text | Table bottom border width for each row (excluding the header) | `0.1rem` | Allow all CSS length units, such as: 48rem, 780px, 70vw, 70%. (shown only when `$is_show_the_table_bottom_border===true`) |
| `heading_margin_top_multiplier` | number | Heading margin-top multiplier | `1` | Set the margin-top multiplier for headings (h1, h2, etc.). Value of 1 means use default margins, less than 1 reduces margins, greater than 1 increases margins |
| `heading_margin_bottom_multiplier` | number | Heading margin-bottom multiplier | `1` | Set the margin-bottom multiplier for headings (h1, h2, etc.). Value of 1 means use default margins, less than 1 reduces margins, greater than 1 increases margins |
| `paragraph_margin_top_multiplier` | number | Paragraph margin-top multiplier | `1` | Set the margin-top multiplier for paragraphs (p). Value of 1 means use default margins, less than 1 reduces margins, greater than 1 increases margins |
| `paragraph_margin_bottom_multiplier` | number | Paragraph margin-bottom multiplier | `1` | Set the margin-bottom multiplier for paragraphs (p). Value of 1 means use default margins, less than 1 reduces margins, greater than 1 increases margins |

## Home page style

Access in templates: `theme.config.index_styles.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `page_html_title` | text | Page title | — | The title of the page, which will be displayed in the browser title bar. |
| `is_enable_canonical_link` | switch | Output canonical link tag | on |  |
| `quote` | switch | Quote fetcher | off |  |
| `quote_link` | text | Quote fetcher link | `https://v1.hitokoto.cn/?encode=text` | Default: https://v1.hitokoto.cn/?encode=text. The endpoint must return plain text and, for cross-origin browser fetch requests, support CORS; otherwise fetching will fail. Default endpoint document… |
| `is_quote_fade_in_animation_enable` | switch | Quote fetcher fade-in animation | on | (shown only when `$quote === true`) |
| `is_random_sentence_show` | switch | Custom random sentence display | off |  |
| `random_sentence_list` | object array | Custom sentence content | — | (shown only when `$is_random_sentence_show === true`) |
| └ `sentence` | text | One Sentence | — |  |
| `is_resume_show` | switch | Personal profile/Announcement | on |  |
| `resume` | code | Personal profile/Announcement content | — | support HTML code block (shown only when `$is_resume_show === true`) |
| `is_i18n_resume_show` | switch | Multi-language personal profile/announcement support | off |  |
| `i18n_resume` | object array | Custom multi-language announcement content | — | (shown only when `$is_i18n_resume_show === true`) |
| └ `lang` | text | Language code | `en` |  |
| └ `resume` | code | Personal profile/Announcement content | — | support HTML code block |
| `is_show_find_me_left_text` | switch | Display the text on the left side of the social media information | on | If enabled, the theme will display the text on the left side of the social media information. |
| `is_show_index_post_list_title` | switch | Enable post list title | on | If enabled, the theme will display the title of the post list. |
| `list_layout` | select | Home page post list layout | `post-list-summary` | The "Moment list" requires the "Moments" plugin to be enabled for proper use, and the "Friends list" requires the "Friends" plugin to be enabled for proper use. |
| `is_show_post_pubdate_in_simple_post_list` | switch | Display publish date in simple post list | on | (shown only when `$list_layout === 'simple-post-list'`) |
| `is_show_post_views_in_simple_post_list` | switch | Display post views in simple post list | off | (shown only when `$list_layout === 'simple-post-list'`) |
| `is_show_post_pubdate_in_post_list_summary` | switch | Display publish date in post list summary | on | (shown only when `$list_layout === 'post-list-summary'`) |
| `is_show_post_categories_in_post_list_summary` | switch | Display post categories in post list summary | on | (shown only when `$list_layout === 'post-list-summary'`) |
| `is_show_post_tags_in_post_list_summary` | switch | Display post tags in post list summary | on | (shown only when `$list_layout === 'post-list-summary'`) |
| `is_show_post_views_in_post_list_summary` | switch | Display post views in post list summary | on | (shown only when `$list_layout === 'post-list-summary'`) |
| `is_show_post_estimated_reading_time_in_post_list_summary` | switch | Display post estimated reading time in post list summary | off | (shown only when `$list_layout === 'post-list-summary'`) |
| `is_show_post_word_count_in_post_list_summary` | switch | Display post word count in post list summary | off | (shown only when `$list_layout === 'post-list-summary'`) |
| `is_show_post_excerpt_in_post_list_summary` | switch | Display post excerpt in post list summary | on | (shown only when `$list_layout === 'post-list-summary'`) |
| `post_excerpt_max_lines` | number | Maximum number of lines in the post list summary | `3` | The value range is 1-5 (shown only when `$list_layout === 'post-list-summary'`) |
| `is_show_index_post_list_permalink_text` | switch | Display the text of the post list permalink | on | If enabled, the theme will display the text of the post list permalink. (shown only when `$list_layout === 'post-list-summary'`) |
| `is_show_post_cover_in_post_list_summary` | switch | Display post cover in the post list summary | on | (shown only when `$list_layout === 'post-list-summary'`) |
| `moment_list_page_size` | number | Number of moments per page | `10` | (shown only when `$list_layout === 'moment-list-summary'`) |
| `is_show_moment_avatar` | switch | Show author avatar in moment list | on | (shown only when `$list_layout === 'moment-list-summary'`) |
| `is_show_moment_nickname` | switch | Show author nickname in moment list | on | (shown only when `$list_layout === 'moment-list-summary'`) |
| `is_show_moment_pubdate` | switch | Show post publication date in moment list | on | (shown only when `$list_layout === 'moment-list-summary'`) |
| `is_show_moment_estimated_reading_time` | switch | Show estimated reading time in moment list | off | (shown only when `$list_layout === 'moment-list-summary'`) |
| `is_show_moment_word_count` | switch | Show word count in moment list | off | (shown only when `$list_layout === 'moment-list-summary'`) |
| `is_moment_upvote_button_show` | switch | Enable upvote button in moment list | on | (shown only when `$list_layout === 'moment-list-summary'`) |
| `is_moment_comment_section_show` | switch | Enable comment section in moment list | on | (shown only when `$list_layout === 'moment-list-summary'`) |
| `friends_list_page_size` | number | Number of items to display in friends list | `10` | (shown only when `$list_layout === 'friends-list-summary'`) |
| `is_show_friend_pubdate` | switch | Show publish date in friends list | on | (shown only when `$list_layout === 'friends-list-summary'`) |
| `is_show_friend_author` | switch | Show author information in friends list | on | Author information includes avatar and name (shown only when `$list_layout === 'friends-list-summary'`) |
| `is_show_friend_author_avatar` | switch | Show author avatar in friends list | on | (shown only when `$list_layout === 'friends-list-summary' && $is_show_friend_author === true`) |
| `is_show_friend_author_name` | switch | Show author name in friends list | on | (shown only when `$list_layout === 'friends-list-summary' && $is_show_friend_author === true`) |
| `is_show_friend_description` | switch | Show post description in friends list | on | (shown only when `$list_layout === 'friends-list-summary'`) |
| `friend_description_max_lines` | number | Maximum lines for post description in friends list | `3` | Valid range is 1-5 (shown only when `$list_layout === 'friends-list-summary' && $is_show_friend_description === true`) |
| `is_show_friend_permalink_text` | switch | Show link text in friends list | on | If disabled, the friends list will not display the link text (shown only when `$list_layout === 'friends-list-summary'`) |
| `is_pin_icon_show` | switch | Top icon display on the post list | on |  |
| `pin_icon_position` | select | Top icon position | `right` | (shown only when `$is_pin_icon_show===true`) |

## Post page style

Access in templates: `theme.config.post_styles.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `is_enable_canonical_link` | switch | Output canonical link tag | on |  |
| `is_optimize_content_paragraph_spacing` | switch | Optimize article paragraph spacing display | off | When enabled, adds minimum height to article content paragraphs to improve blank line display |
| `is_enable_paragraph_first_line_indent` | switch | Enable paragraph first-line indent | off | When enabled, article paragraphs will use first-line indent style, typically 2 character widths (2em) |
| `paragraph_first_line_indent_value` | text | First-line indent value | `2em` | Set the size of the first-line indent. All CSS length units are allowed, such as: 2em, 32px, 2rem, etc. Recommended: 2em (shown only when `$is_enable_paragraph_first_line_indent === true`) |
| `post_title_uppper` | switch | Post title uppercase | off |  |
| `is_show_post_publish_time` | switch | Post publish time | on |  |
| `is_show_post_publish_time_left_text` | switch | Post publish time left text | off | (shown only when `$is_show_post_publish_time === true`) |
| `is_show_post_updated_time` | switch | Post update time | off |  |
| `is_show_post_updated_time_left_text` | switch | Post update time left text | off | (shown only when `$is_show_post_updated_time === true`) |
| `is_show_post_views` | switch | Show post views | on |  |
| `is_show_post_estimated_reading_time` | switch | Show estimated reading time of the article | off |  |
| `is_show_post_word_count` | switch | Show post word count | off |  |
| `is_show_post_nav_share_button` | switch | Share button in desktop menu | on |  |
| `heading_anchor_symbol` | text | Heading anchor symbol | — | Customize the symbol displayed before heading anchor links on post and single pages. Leave empty to use the default #. Supports any string, e.g. § ¶ 🔗. This setting is overridden when a heading anc… |
| `is_heading_anchor_symbol_raw` | switch | Heading anchor symbol raw output | off | When enabled, the heading anchor symbol value above is set directly as the CSS variable value without quoting. Intended for users familiar with CSS content property syntax. |
| `heading_anchor_svg` | icon | Heading anchor icon | — | When set, overrides the heading anchor symbol setting above. |
| `is_custom_toc_max_width` | switch | Custom sidebar table of contents maximum width | off | When enabled, you can set the maximum width of the table of contents sidebar on article pages. |
| `toc_max_width` | text | Sidebar table of contents maximum width | `20rem` | Controls the maximum width of the table of contents sidebar on article pages. All CSS length units are allowed, such as: 20rem, 300px, 30vw. (shown only when `$is_custom_toc_max_width === true`) |
| `is_dividing_line_at_the_end_of_post_show` | switch | Enable dividing line at the end of the post | on |  |
| `is_post_upvote_button_show` | switch | Upvote button at the bottom of the post | off |  |
| `post_upvote_button_width` | text | Upvote icon width | `1em` | Set the width of the heart icon (the button itself is a content-width pill). All CSS length units are allowed; 1em follows the button's font size. (shown only when `$is_post_upvote_button_show === … |
| `post_upvote_button_height` | text | Upvote icon height | `1em` | Set the height of the heart icon (the button itself is a content-width pill). All CSS length units are allowed; 1em follows the button's font size. (shown only when `$is_post_upvote_button_show ===… |
| `is_show_post_upvote_count` | switch | Display post upvote count | on | (shown only when `$is_post_upvote_button_show === true`) |
| `post_upvote_button_position` | select | Upvote button position | `center` | (shown only when `$is_post_upvote_button_show === true`) |
| `is_post_recommended_articles_show` | switch | Recommended articles at the bottom of the post | off | When enabled, recommended articles will be displayed at the bottom of the post. Note: This works by reading the first category of the current article and randomly outputting several articles from i… |
| `post_recommended_articles_count` | number | Number of recommended articles | `3` | Set the number of recommended articles displayed at the bottom of the post (shown only when `$is_post_recommended_articles_show === true`) |
| `is_post_prev_next_navigation_show` | switch | Adjacent article navigation at the bottom of the post | off | When enabled, navigation links to the previous and next articles will be displayed at the bottom of the post |
| `is_post_comment_section_show` | switch | Enable comment section | on |  |
| `is_show_footer_nav` | switch | Mobile footer navigation | on |  |
| `is_show_home_footer` | switch | Home button in the mobile footer navigation | on | (shown only when `$is_show_footer_nav === true`) |
| `is_show_share_footer` | switch | Share buttons in the mobile footer navigation | on | (shown only when `$is_show_footer_nav === true`) |

## Category page style

Access in templates: `theme.config.categories_page_styles.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `description` | text | Page description (meta description) | — | Used as the meta description; falls back to site SEO description when empty |
| `is_enable_canonical_link` | switch | Output canonical link tag | on |  |
| `is_show_the_number_of_articles_per_category` | switch | Display the number of articles per category | on |  |
| `characters_to_the_left_of_the_number_of_posts` | text | Characters to the left of the number of articles | `(` | (shown only when `$is_show_the_number_of_articles_per_category === true`) |
| `characters_to_the_right_of_the_number_of_posts` | text | Characters to the right of the number of articles | `)` | (shown only when `$is_show_the_number_of_articles_per_category === true`) |
| `is_show_multi_layer_categories` | switch | Display multi-layer categories | on |  |

## Category details page style

Access in templates: `theme.config.category_page_styles.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `is_enable_canonical_link` | switch | Output canonical link tag | on |  |
| `is_show_rss_button` | switch | Show RSS subscription button | off | Requires enabling the RSS plugin (https://www.halo.run/store/apps/app-KhIVw) |
| `is_show_post_pubdate_in_post_list` | switch | Display post publish date in post list | on |  |
| `is_show_post_views_in_post_list` | switch | Display post views in post list | off |  |

## Tag Collection Page Style

Access in templates: `theme.config.tags_page_styles.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `description` | text | Page description (meta description) | — | Used as the meta description; falls back to site SEO description when empty |
| `is_enable_canonical_link` | switch | Output canonical link tag | on |  |
| `is_show_the_number_of_posts_per_tag` | switch | Display the number of posts per tag | on |  |
| `characters_to_the_left_of_the_number_of_posts` | text | Characters to the left of the number of posts | `(` | (shown only when `$is_show_the_number_of_posts_per_tag === true`) |
| `characters_to_the_right_of_the_number_of_posts` | text | Characters to the right of the number of posts | `)` | (shown only when `$is_show_the_number_of_posts_per_tag === true`) |
| `tags_sort_order` | select | Tag sort order | `default` |  |

## Tag details page style

Access in templates: `theme.config.tag_page_styles.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `is_enable_canonical_link` | switch | Output canonical link tag | on |  |
| `is_show_rss_button` | switch | Show RSS subscription button | off | Requires enabling the RSS plugin (https://www.halo.run/store/apps/app-KhIVw) |
| `is_show_post_pubdate_in_post_list` | switch | Display post publish date in post list | on |  |
| `is_show_post_views_in_post_list` | switch | Display post views in post list | off |  |

## Author details page style

Access in templates: `theme.config.author_page_styles.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `is_enable_canonical_link` | switch | Output canonical link tag | on |  |
| `is_show_rss_button` | switch | Show RSS subscription button | off | Requires enabling the RSS plugin (https://www.halo.run/store/apps/app-KhIVw) |
| `is_show_post_pubdate_in_post_list` | switch | Display post publish date in post list | on |  |

## Archives page style

Access in templates: `theme.config.archives_page_styles.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `description` | text | Page description (meta description) | — | Used as the meta description; falls back to site SEO description when empty |
| `is_enable_canonical_link` | switch | Output canonical link tag | on |  |
| `is_show_post_pubdate_in_post_list` | switch | Display post publish date in post list | on |  |
| `is_collapse_post_list_by_publication_year_and_month` | switch | Collapse post list by publication year and month | off |  |
| `duration_of_expand_collapse_animation` | number | Duration of expand/collapse animation | `200` | Unit: milliseconds (shown only when `$is_collapse_post_list_by_publication_year_and_month === true`) |

## Custom page style

Access in templates: `theme.config.custom_page_styles.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `is_enable_canonical_link` | switch | Output canonical link tag | on |  |
| `is_optimize_content_paragraph_spacing` | switch | Optimize article paragraph spacing display | off | When enabled, adds minimum height to article content paragraphs to improve blank line display |
| `is_enable_paragraph_first_line_indent` | switch | Enable paragraph first-line indent | off | When enabled, article paragraphs will use first-line indent style, typically 2 character widths (2em) |
| `paragraph_first_line_indent_value` | text | First-line indent value | `2em` | Set the size of the first-line indent. All CSS length units are allowed, such as: 2em, 32px, 2rem, etc. Recommended: 2em (shown only when `$is_enable_paragraph_first_line_indent === true`) |
| `is_show_post_estimated_reading_time` | switch | Show estimated reading time of the article | off |  |
| `is_show_post_word_count` | switch | Show word count of the article | off |  |
| `is_dividing_line_at_the_end_of_content_show` | switch | Enable dividing line at the end of the content | on |  |
| `is_custom_page_comment_section_show` | switch | Enable comment section | on |  |

## Error Page Style

Access in templates: `theme.config.error_page_styles.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `is_show_error_detail` | switch | Show error detail | off | The detail string comes from the server framework (e.g. "No static resource ..." on a 404). It does not help visitors and leaks internals. Recommended only during development. |
| `is_auto_redirect` | switch | Page auto-redirect | off |  |
| `target_url` | text | Redirect target URL | `/` | (shown only when `$is_auto_redirect === true`) |
| `redirect_wait_time` | number | Redirect wait time | `5` | Set the wait time for redirect, in seconds. (shown only when `$is_auto_redirect === true`) |

## Social media settings/RSS

Access in templates: `theme.config.sns.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `sns_list` | object array | Social media settings | — | You can set up your social media information here, and the theme will automatically generate the corresponding link. |
| └ `type` | select | Type | `custom_sns` | Enter the id / Enter any text / Enter the feed address, e.g., /rss.xml / Enter the UID only / Enter the username only / Enter the email address only / Enter the username only / Enter the username o… |
| └ `input_value` | text | Value | — |  |
| `custom_sns` | object array | Custom social media information | — | You can define your own social media information here. |
| └ `id` | text | ID | — | The unique identifier of the social media information, such as: github2, twitter2. |
| └ `url` | text | URL | — | The URL of the social media information, such as: https://example.com |
| └ `icon` | icon | Icon | — |  |
| └ `aria_label` | text | aria-label | — | The aria-label of the social media information, such as: Find me on Github |

## Custom Share Buttons

Access in templates: `theme.config.share.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `share_list` | object array | Share Button List | `[{'type': 'native'}, {'type': 'email'}, {'type': 'qrcode'}, {'type': 'facebook'}, {'type': 'reddit'}, {'type': 'x'}, {'type': 'linkedin'}, {'type': 'pinterest'}, {'type': 'telegram'}, {'type': 'whatsapp'}, {'type': 'tumblr'}, {'type': 'blogger'}, {'type': 'gmail'}, {'type': 'yahoomail'}, {'type': 'skype'}, {'type': 'line'}, {'type': 'hackernews'}, {'type': 'qq'}, {'type': 'weibo'}, {'type': 'wechat'}, {'type': 'qzone'}, {'type': 'douban'}]` | Controls the display and order of share buttons |
| └ `type` | select | Type | `native` |  |
| └ `icon` | icon | Icon | — | Overrides the default icon when set |
| └ `custom_id` | text | Identifier | — | Enter the identifier of the custom button defined below (shown only when `$get(share_list_type).value === 'custom_share'`) |
| `custom_share` | object array | Custom Share Buttons | — | @URL and @TITLE are placeholders that will be replaced with the actual page URL and title. After defining here, remember to add it in the list above. |
| └ `id` | text | Identifier | — | Any combination of letters, numbers, and underscores, e.g.: myShare |
| └ `name` | text | Name | — |  |
| └ `url` | text | URL | — |  |
| └ `icon` | icon | Icon | — |  |
| └ `aria_label` | text | aria-label | — | The name in the aria-label |

## Links Page Style

Access in templates: `theme.config.links_page_styles.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `is_enable_canonical_link` | switch | Output canonical link tag | on |  |
| `is_head_first_style` | switch | Avatar-First Style | off |  |
| `link_description_max_lines` | number | Link Description Maximum Lines | `2` | Value range: 1-5 (shown only when `$is_head_first_style === true`) |

## Photo Page Style

Access in templates: `theme.config.photos_styles.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `is_enable_canonical_link` | switch | Output canonical link tag | on |  |
| `img_border_radius` | text | Image border radius | `8px` | Set the border radius of the image. All CSS length units are allowed, such as: 0px, 5px, 10%, 1rem. |
| `img_transition_duration_after_load` | number | Image fade-in animation duration | `0.2` | Set the image fade-in animation duration, in seconds. |
| `is_show_photo_group_name` | switch | Group title | off |  |
| `is_enable_masonry_layout` | switch | Masonry layout | on |  |
| `max_masonry_columns` | number | Maximum number of masonry columns | `3` | Set the maximum number of masonry columns. (shown only when `$is_enable_masonry_layout === true`) |
| `min_masonry_columns` | number | Minimum number of masonry columns | `2` | Set the minimum number of masonry columns. (shown only when `$is_enable_masonry_layout === true`) |
| `min_img_width` | number | Minimum width of images in masonry layout | `300` | Set the minimum width of images in masonry layout. Unit: px (shown only when `$is_enable_masonry_layout === true`) |
| `masonry_gap` | number | Masonry gap width | `9` | Set the gap width for the masonry layout. Unit: px (shown only when `$is_enable_masonry_layout === true`) |
| `is_enable_advanced_options` | switch | Advanced configuration options | off | This configuration item requires frontend knowledge. |
| `img_onmouseover` | text | Custom image onmouseover attribute | `this.style.boxShadow='0 0 7.5px var(-…` | Default value: this.style.boxShadow='0 0 7.5px var(--color-accent), 0 0 7.5px var(--color-accent)'; (shown only when `$is_enable_advanced_options === true`) |
| `img_onmouseout` | text | Custom image onmouseout attribute | `this.style.boxShadow='none';` | Default value: this.style.boxShadow='none'; (shown only when `$is_enable_advanced_options === true`) |

## Moments page style

Access in templates: `theme.config.moments_styles.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `is_enable_canonical_link` | switch | Output canonical link tag | on |  |
| `is_show_post_pubdate` | switch | Show post publication date | on |  |
| `is_show_post_estimated_reading_time` | switch | Show estimated reading time of the post | off |  |
| `is_show_post_word_count` | switch | Show word count of the post | off |  |
| `is_moment_upvote_button_show` | switch | Enable upvote button | on |  |
| `is_moment_comment_section_show` | switch | Enable comment section | on |  |

## Friends page style

Access in templates: `theme.config.friends_page_styles.<setting name>`

| Setting | Type | Label | Default | Description |
| --- | --- | --- | --- | --- |
| `is_enable_canonical_link` | switch | Output canonical link tag | on |  |
| `is_show_friend_pubdate` | switch | Show publish date | on |  |
| `is_show_friend_author` | switch | Show author information | on | Author information includes author avatar and name |
| `is_show_friend_author_avatar` | switch | Show author avatar | on | (shown only when `$is_show_friend_author === true`) |
| `is_show_friend_author_name` | switch | Show author name | on | (shown only when `$is_show_friend_author === true`) |
| `is_show_friend_description` | switch | Show post description | on |  |
| `friend_description_max_lines` | number | Maximum lines for post description | `3` | Valid range is 1-5 (shown only when `$is_show_friend_description === true`) |
| `is_show_friend_permalink_text` | switch | Show link text | on | If disabled, the friends list will not display link text |

