# Theme Auto Gray Component

## Description

Provides CSS variable definitions for the auto gray theme component: the light gray tokens
plus a `prefers-color-scheme: dark` block carrying the dark gray tokens.

## Usage

Head Content (for including necessary scripts/styles)

```html
<th:block th:insert="~{components/theme-auto-gray/template :: head}"></th:block>
```
